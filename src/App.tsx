import { Button } from "@blueprintjs/core";

function App() {
  return (
    <div className="app">
      <Button intent="primary" small={true}>
        Hello (small) world!
      </Button>
    </div>
  );
}

export default App;
